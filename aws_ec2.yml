plugin: aws_ec2
regions:
  - us-east-1
filters:
  tag:Name: 
    - build
    - deploy
keyed_groups:
  - key: tags.Name
    prefix: ''
compose:
  ansible_user: ubuntu
  ansible_ssh_private_key_file: /home/user/.ssh/user18kp.pem